name: Purple Cloud SOL 30M SL3 TP3
on:
  workflow_dispatch:
jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pandas numpy requests
      - run: python -m backtest.purple_cloud_sol_30m_sl3_tp3
      - uses: actions/upload-artifact@v4
        with:
          name: purple-cloud-sol-30m-sl3-tp3-results
          path: |
            backtest/data/purple_cloud_sol_30m_sl3_tp3_summary.csv
            backtest/data/purple_cloud_sol_30m_sl3_tp3_detail.csv
